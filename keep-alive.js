export default async function handler(req, res) { try { const response = await fetch( "https://sdbobbmqudrhscotfnok.supabase.co/functions/v1/ping" ); const data = await response.text(); res.status(200).json({ ok: true, status: response.status, data }); } catch (error) { res.status(500).json({ ok: false, error: error.message }); } }
